apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastapi-celery-app.fullname" . }}
  labels:
    {{- include "fastapi-celery-app.labels" . | nindent 4 }}
    {{- with .Values.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fastapi-celery-app.selectorLabels" . | nindent 4 }}